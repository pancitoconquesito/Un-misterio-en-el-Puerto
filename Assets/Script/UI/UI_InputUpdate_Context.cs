using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class UI_InputUpdate_Context : MonoBehaviour
{
    [SerializeField] UI_InputUpdate_Gameplay m_gameplay;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    //public void UpdateSprites(InputDeviceDetector.TYPE_INPUT m_curr_input)
    //{
    //    m_gameplay.UpdateSprites(m_curr_input);
    //}
}
